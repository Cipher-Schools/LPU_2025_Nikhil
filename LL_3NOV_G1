// Online C++ compiler to run C++ program online
#include <bits/stdc++.h>
using namespace std;

struct Node{
    int data;
    Node* next;
    
    Node(int value){
        data = value;
        next = NULL;
    }
};

void PrintLL( Node* head){
    Node* p = head;
    
    while(p!=NULL){
        cout<<p->data<<" ";
        p = p->next;
    }
}


int main() {
    
    Node* head = new Node(12);
    head->next = new Node(22);
    head->next->next = new Node(32);
    head->next->next->next = new Node(42);
    head->next->next->next->next = new Node(52);
     head->next->next->next->next->next = head->next->next; 
     //PrintLL(head);
     cout<<endl;
     
     //Find Mid with two loops 
     //TC - > O(2*N) ~ O(N)
     
    //  int count = 0;
     
     
    //  Node* p = head;
     
    //  while(p!=nullptr){
    //      count++;
    //      p = p->next;
    //  }
     
    //  int mid = count/2;
    //  p = head;
     
    //  for(int i = 0 ;i<mid;i++){
    //      p = p->next;
    //  }
     
    //  cout<<p->data;
     
     
     
       //Find Mid with ONE loops  ( SLow And Fast Pointers)
     //TC - > O(N)
     
     
    //  Node* fast = head;
    //  Node* slow = head;
     
    //  while(fast->next!=nullptr && fast->next->next!=nullptr){
    //      fast = fast->next->next;
    //      slow = slow->next;
    //  }
     
    //  cout<<slow->data;
     
     
    //  Q-> Detect a loop in LL
     
    //   Node* fast = head;
    //  Node* slow = head;
     
    //  int flag = 0;
     
    //  do{
    //      slow=slow->next;
    //      fast=fast->next->next;
         
    //      if(slow==fast){
    //          flag = 1;
    //          break;
    //      }
         
    //  }while(fast!=nullptr &&  fast->next!=nullptr);
     
     
    //  if(flag){
    //      cout<<"YES";
    //  }
    //  else cout<<"NO";
    
    
    // Q->Detect the starting point of loop in LL
    
      Node* fast = head;
     Node* slow = head;
    
     do{
         slow=slow->next;
         fast=fast->next->next;
         
         if(slow==fast){
            
            slow = head;
            int position = 1;
            while(slow!=fast){
                slow=slow->next;
                fast=fast->next;
                position++;
                
            }
            // cout<<slow->data;
            cout<<position;
            break;
         }
         
     }while(fast!=nullptr &&  fast->next!=nullptr);
     
     
     
     
     


    return 0;
}
